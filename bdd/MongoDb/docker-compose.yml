version: "3.3"
services:
  mongodb:
    image : mongo:7.0
    container_name: jojo
    environment:
      MONGO_INITDB_ROOT_USERNAME: jojo
      MONGO_INITDB_ROOT_PASSWORD: jojo
    volumes:
      - ./jojo:/data/db
    ports:
      - 27017:27017
