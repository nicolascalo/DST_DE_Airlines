FROM python:3.12

WORKDIR /app


RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*


COPY download_collection_to_input_data.sh .

RUN chmod +x download_collection_to_input_data.sh

ENV PYTHONUNBUFFERED=1

CMD ["./download_collection_to_input_data.sh"]
