FROM python:3.12

WORKDIR /app


RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*


COPY insert_data_into_postgres.sh .

RUN chmod +x insert_data_into_postgres.sh

ENV PYTHONUNBUFFERED=1

CMD ["./insert_data_into_postgres.sh"]
